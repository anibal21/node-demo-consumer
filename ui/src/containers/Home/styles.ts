import { makeStyles } from '@material-ui/styles'

export const homeStyles = makeStyles({
    container: {
        height: '100%',
        width: '100%',
    },
    header: {
        width: '100%',
    },
    table: {
        height: 'calc(100% - 300px)',
        width: '100%',
    }
})