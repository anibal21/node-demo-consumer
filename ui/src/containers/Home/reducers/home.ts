const initialState: any = {
    choosenTab: 0,
    viewType: 0,
}

const reducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer
